# 小说设定库 - Claude 协作指南

## 项目概述
这是一部西幻小说的 Obsidian 设定库，架构专为 AI 检索优化。

## 叙事分层规则（当前口径）

角色信息目前按三类文件存放，默认按下面方式组织：

| 文件 | 存什么 | 例 |
|------|--------|------|
| `char-<名字>.md` | 角色当前主档：记录**已确认且可归入当前正史**的信息 | 伊利安的当前身份与能力、诺蕾莉安当前关系状态 |
| `arc-<名字>-弧线.md`（或 `-信息披露.md`、`-成长弧.md`） | 剧情组织层：同时记录**已确认剧情**、**未揭露真相**、**参考设计** | 诺蕾"关键退后"事件、索妮娅未揭露真相、奥菲收徒设想 |
| `relationships-女主群像.md` | 女主之间的结构性调性关系（一接触就持续存在的动态） | 诺蕾↔菲拉的"投缘"调性 |

**三条默认约定**：
1. **主档原则**：`char` 会随着剧情推进持续完善，记录当前已确认、可写入正史的角色信息
2. **剧透隔离原则**：作者已确认但正文暂未揭露的真相，不直接写回 `char`，继续留在 `plot/arc`
3. **同文件分区原则**：`arc` 文件内部统一分成 `已确认内容`、`参考设计`、`待定事项`，避免把候选剧情误写成定案

## Claude 的工作流程

### 查找信息时
1. 先读 `00-INDEX/_MASTER-INDEX.md` 获取全局视图
2. 根据需要读对应子域的 `_index.md` 深入某个领域（如 `01-WORLD/magic/_index.md`、`05-PLOT/_index.md`）
3. 读具体的设定文件
4. **查角色信息时：先读 `char-<名字>.md` 获取当前角色状态；再读对应 `arc-<名字>-*.md` 的 `已确认内容` 与 `参考设计`，判断剧情阶段和候选走向**

### 创建新内容时
1. 使用 `_TEMPLATES/` 下的对应模板
2. 文件名使用前缀系统（char-、geo-、fac-、item- 等）
3. 完整填写 frontmatter，特别是 summary、related、arc_file 字段
4. 更新对应的 `_index.md` 文件
5. 更新关联文件的双向引用
6. **写 char 卡时遵守当前分层规则**：已确认正史写回 `char`，未揭露真相和候选剧情放 `arc`，女主间结构性关系放 `relationships`

### 写作正文时
1. 先读涉及角色的 `char-` 文件，确认该角色的当前主档状态
2. 再读 `arc-<涉及角色>-*.md`，区分其中的 `已确认内容`、`未揭露真相` 与 `参考设计`
3. 根据当前正文已揭露范围判断"谁知道什么"，不要把 `参考设计` 当成已发生剧情
4. 读取相关地点的 `geo-` 文件
5. 确保正文与设定一致

## 文件命名前缀
- char-（角色）、geo-（地理）、hist-（历史）、magic-（魔法）
- culture-（文化）、religion-（宗教）、politics-（政治）
- fac-（势力）、item-（物品）、arc-（故事弧）
- ch-（章节）、timeline-（时间线）、subplot-（支线）

## 规范
- 所有交叉引用使用 [[wikilink]] 语法
- 修改关系时双向更新
- 单文件不超过 500 行
- 设定文件用客观百科风格，正文用小说叙事风格
