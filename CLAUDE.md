# 小说设定库 - Claude 协作指南

## 项目概述
这是一部西幻小说的 Obsidian 设定库，架构专为 AI 检索优化。

## 叙事分层原则（极重要，2026-04-17 确立）

角色信息分三类文件存放，**AI 读写时必须遵守**：

| 文件 | 存什么 | 例 |
|------|--------|------|
| `char-<名字>.md` | 故事开局时就为真的恒定内容（身份、性格机制、已生效能力、开局关系一句话） | 诺蕾莉安是精灵长老之女、害羞过载机制、义姐身份 |
| `arc-<名字>-弧线.md`（或 `-信息披露.md`、`-成长弧.md`） | 剧情推进中才发生/揭示的内容（成长轨迹、关系演变、隐藏真相披露节奏、具体事件） | 诺蕾"关键退后"事件、索妮娅真相爆发、奥菲收徒 |
| `relationships-女主群像.md` | 女主之间的结构性调性关系（一接触就持续存在的动态） | 诺蕾↔菲拉的"投缘"调性 |

**三条铁律**：
1. **恒定真相**：char 卡只写开局就为真的内容。任何"之后才发生 / 之后才揭示 / 之后才改变 / 成长结果"→ 不进 char 卡，走 arc
2. **确定性**：不确定的内容留字段但值写「待定 · 未决 · 原因」；整段不确定的章节直接删。**严禁推测性填写**（曾经的"阶位约在见习阶（推测）"这类写法禁止）
3. **单一来源**：char / arc / relationships 三处内容不重复；交叉用 wikilink 指针

## Claude 的工作流程

### 查找信息时
1. 先读 `00-INDEX/_MASTER-INDEX.md` 获取全局视图
2. 根据需要读对应子域的 `_index.md` 深入某个领域（如 `01-WORLD/magic/_index.md`、`05-PLOT/_index.md`）
3. 读具体的设定文件
4. **查角色信息时：同时读 `char-<名字>.md`（开局）+ 对应 `arc-<名字>-*.md`（演变）。只读 char 卡会不知剧情，只读 arc 会不知开局**

### 创建新内容时
1. 使用 `_TEMPLATES/` 下的对应模板
2. 文件名使用前缀系统（char-、geo-、fac-、item- 等）
3. 完整填写 frontmatter，特别是 summary、related、arc_file 字段
4. 更新对应的 `_index.md` 文件
5. 更新关联文件的双向引用
6. **写 char 卡时严格遵守叙事分层原则**：剧情内容放 arc，女主间关系放 relationships，不确定的内容写「待定 · 未决」，**严禁推测**

### 写作正文时
1. 先读 `arc-<涉及角色>-*.md` 了解剧情结构与披露节奏
2. 再读涉及角色的 `char-` 文件（开局真相）
3. 对照披露节奏确认当前章节"谁知道什么"——**违反披露节奏会毁掉叙事张力**
4. 读取相关地点的 `geo-` 文件
5. 确保正文与设定一致

## 文件命名前缀
- char-（角色）、geo-（地理）、hist-（历史）、magic-（魔法）
- culture-（文化）、religion-（宗教）、politics-（政治）
- fac-（势力）、item-（物品）、arc-（故事弧）
- ch-（章节）、timeline-（时间线）、subplot-（支线）

## 规范
- 所有交叉引用使用 [[wikilink]] 语法
- 修改关系时双向更新
- 单文件不超过 500 行
- 设定文件用客观百科风格，正文用小说叙事风格
