# 小说设定库 - Claude 协作指南

## 项目概述
这是一部西幻小说的 Obsidian 设定库，架构专为 AI 检索优化。

## 叙事分层规则（当前口径）

角色信息目前按四类文件存放，默认按下面方式组织：

| 文件 | 存什么 | 例 |
|------|--------|------|
| `char-<名字>.md` | 角色当前主档：记录**已确认且可归入当前正史**的信息 | 伊利安的当前身份与能力、诺蕾莉安当前关系状态 |
| `05-PLOT/arcs/arc-<名字>.md` | 剧情组织层：成长方向、关系演变、灵感场景（**参考性质，不是定式**） | 诺蕾"关键退后"灵感、奥菲收徒设想 |
| `05-PLOT/foreshadowing/fore-*.md` | **伏笔 / 灵感池**：记下"有这么个伏笔/真相"，不预排时序 | 索妮娅神选者身份、奥菲莉娅真实年龄 |
| `relationships-女主群像.md` | 女主之间的结构性调性关系（一接触就持续存在的动态） | 诺蕾↔菲拉的"投缘"调性 |

**四条默认约定**：
1. **主档原则**：`char` 会随着剧情推进持续完善，记录当前已确认、可写入正史的角色信息
2. **剧透隔离原则**：作者已确认但正文暂未揭露的真相，不直接写回 `char`，继续留在 `plot/arc` 或 `foreshadowing/`
3. **同文件分区原则**：`arc` 文件内部统一分成 `已确认内容`、`参考设计`、`待定事项`；`参考设计` 区全部是灵感/候选，不是定案
4. **伏笔独立原则**：所有隐藏真相、未揭露内幕统一抽到 `05-PLOT/foreshadowing/` 独立管理；只记"存在什么伏笔"，不预排"什么时候埋/回收"；arc 和 char 均不直接承载真相清单，只用 wikilink 指过去

## Claude 的工作流程

### 查找信息时
1. 先读 `00-INDEX/_MASTER-INDEX.md` 获取全局视图
2. 根据需要读对应子域的 `_index.md` 深入某个领域（如 `01-WORLD/magic/_index.md`、`05-PLOT/_index.md`）
3. 读具体的设定文件
4. **查角色信息时：先读 `char-<名字>.md` 获取当前角色状态；再读对应 `05-PLOT/arcs/arc-<名字>.md` 的 `已确认内容` 与 `参考设计`，判断剧情阶段和候选走向；查伏笔/隐藏真相时读 `05-PLOT/foreshadowing/_index.md`**

### 创建新内容时
1. 使用 `_TEMPLATES/` 下的对应模板
2. 文件名使用前缀系统（char-、geo-、fac-、item- 等）
3. 完整填写 frontmatter，特别是 summary、related、arc_file 字段
4. 更新对应的 `_index.md` 文件
5. 更新关联文件的双向引用
6. **写 char 卡时遵守当前分层规则**：已确认正史写回 `char`，候选剧情/灵感场景放 `arcs/arc-*`，隐藏真相/伏笔放 `foreshadowing/fore-*`，女主间结构性关系放 `relationships`

### 写作正文时
1. 先读涉及角色的 `char-` 文件，确认该角色的当前主档状态
2. 再读 `05-PLOT/arcs/arc-<涉及角色>.md`，区分其中的 `已确认内容`（定式）与 `参考设计`（灵感候选）；需要查伏笔时读 `05-PLOT/foreshadowing/_index.md`
3. 根据当前正文已揭露范围判断"谁知道什么"，不要把 `参考设计` 当成已发生剧情
4. 读取相关地点的 `geo-` 文件
5. 确保正文与设定一致

## 文件命名前缀
- char-（角色）、geo-（地理）、hist-（历史）、magic-（魔法）
- culture-（文化）、religion-（宗教）、politics-（政治）
- fac-（势力）、item-（物品）、arc-（故事弧）
- ch-（章节）、timeline-（时间线）、subplot-（支线）

## 规范
- 所有交叉引用使用 [[wikilink]] 语法
- 修改关系时双向更新
- 单文件不超过 500 行
- 设定文件用客观百科风格，正文用小说叙事风格

---

## 2026-09 补充：正文写作口径（必须遵守）

1. **方向**：西幻 · 剑与魔法 · 后宫轻喜剧。魔法只写仪式/代价/观感，**禁止一切理科描述**（数值、公式、参数、蓝条、回路、原理说明），详见 [[07-REFERENCES/style-网文文风手册]] §3.6。
2. **文风**：中文网文语言。反AI味硬性规则（副词、空镜句、AI句式黑名单、每章自检清单）见文风手册 §3 与 §5。成稿后用 story-deslop / humanizer-zh 技能过一遍。
3. **工作流**：写正文前查 [[07-REFERENCES/前情提要-正文状态]] 与 [[volumes/volume-01-详纲]]；写后同步两者与 06-CHAPTERS/_index.md。详纲是导航不是枷锁。
4. **单章**：2000–2400 字，章末留钩。不水字数。
5. **人名地名**：与 [[07-REFERENCES/naming-规则]] 及已使用名称索引保持一致；新增命名先查重。
