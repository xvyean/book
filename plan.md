# 小说设定库当前建设 plan

## Context

本项目是一部西幻小说的 Obsidian 设定库。当前唯一主工作区是仓库内的 `new/`，后续建设、补强、巡检、索引维护都以它为准。

旧结构目录、`.claude/drafts/`、memory 仍然可以作为参考来源，但它们不再决定当前库的目录、模板和写法，也不再作为逐文件迁移清单使用。

本 plan 的目的，是把当前已经稳定下来的目录、模板、索引和执行口径写成一份可持续使用的工作总计划，方便后续的人类或 AI 在同一套规则上继续扩写，而不是反复回到旧迁移思路。

---

## 一、当前工作模式

### 1.1 基本原则

- 以 `new/` 现状为唯一主库，不回退到旧结构思路。
- 模板保持轻量、条目式、客观百科风格，不追求大而全。
- 剧透继续依靠文件位置和域分层隔离，不把隐藏真相混写进当前正史主档。
- 已命名但暂不值得独立建档的内容，可以只留在父档正文或总索引里，不建空壳文件。

### 1.2 当前顶层结构

```text
new/
├── 00-导航/
├── 01-角色/
├── 02-世界/
├── 03-剧情/
├── 04-正文/
├── 05-资料/
├── 90-规则/
└── _TEMPLATES/
```

### 1.3 当前工作顺序

1. 先读相关域的 `_index.md` 确认现状与入口。
2. 再读对应模板，确认 frontmatter 和正文骨架。
3. 在现有主档基础上补强，不为了“补全”而新开空文件。
4. 改动正文时同步维护 `_index.md`、父子链和必要的 `[[wikilink]]`。

---

## 二、目录、命名与索引规则

### 2.1 前缀规范

| 前缀 | 类型 | 主要目录 | 示例 |
|------|------|----------|------|
| `char-` | 角色 | `new/01-角色/**` | `char-伊利安.md` |
| `geo-` | 地点 | `new/02-世界/地点/**` | `geo-艾瑟维伦.md` |
| `race-` | 智慧种族 | `new/02-世界/智慧种族/` | `race-精灵.md` |
| `deity-` | 神明 | `new/02-世界/神明/` | `deity-太阳之神·伊塞拉.md` |
| `item-` | 物品 | `new/02-世界/物品/` | `item-无字魔典.md` |
| `fac-` | 组织 / 势力 | `new/02-世界/组织/` | `fac-奥尔德林学院.md` |
| `creature-` | 生灵 / 精怪 / 魔物 | `new/02-世界/生灵/` | `creature-林风精灵.md` |
| `magic-` | 魔法规则 | `new/02-世界/规则/魔法/` | `magic-魔导卡总览.md` |
| `law-` | 法则 / 形而上规则 | `new/02-世界/规则/法则/` | `law-神选与神役.md` |
| `hist-` | 历史 / 纪元 | `new/02-世界/历史/` | `hist-纪元总览.md` |
| `fore-` | 伏笔 | `new/03-剧情/伏笔/` | `fore-索妮娅神选身份.md` |
| `arc-` | 剧情纲要 / 专题剧情弧 | `new/03-剧情/` | `arc-总纲.md` |
| `timeline-` | 时间线 | `new/03-剧情/` | `timeline-时间线.md` |
| `vol-` | 卷纲 | `new/03-剧情/分卷/**` | `vol-第一卷.md` |
| `scene-` | 分幕 | `new/03-剧情/分卷/**/分幕/` | `scene-第一卷-01-林地到学院.md` |
| `ch-` | 正文章节 | `new/04-正文/**` | `ch-001-初见.md` |

### 2.2 剧情域当前结构与前缀落点

剧情域继续保留当前已经拆好的层级结构，不回退成扁平 `arc-*` 文件制：

```text
new/03-剧情/
├── arc-总纲.md
├── timeline-时间线.md
├── 全局灵感池.md
├── 伏笔/
│   ├── _index.md
│   └── fore-*.md
└── 分卷/
    └── 第N卷/
        ├── vol-第N卷.md
        └── 分幕/
            └── scene-第N卷-XX-标题.md
```

当前口径如下：

- `arc-` 用于剧情域正式纲要类文件，当前默认落点是 `arc-总纲.md`。
- `timeline-` 用于时间线文件。
- `vol-` 用于每卷卷纲文件，保留 `分卷/第N卷/` 目录层。
- `scene-` 用于分幕文件，命名中显式带卷次，避免跨卷同名冲突。
- `fore-` 用于正式伏笔文件；`伏笔/_index.md` 继续作为该域索引。
- 前缀是加在现有正式文件名上，不是另起一套平行目录。

### 2.3 不加前缀的例外

- 所有 `_index.md`
- `new/00-导航/` 下的导航文件
- `new/05-资料/` 下的手册类文件
- `new/04-正文/场景碎片/` 与 `new/04-正文/废稿/` 下的自由草稿
- `new/03-剧情/全局灵感池.md`（自由灵感聚合页，不视为正式剧情主档）
- 其他经确认属于自由灵感、而非正式设定主档的文件

### 2.4 `_index.md` 规则

- 一般域索引使用“入口 + 一句话定位”的简洁写法。
- 地点域索引是唯一例外：`new/02-世界/地点/_index.md` 必须承担全地点目录树职责。
- 索引服务于检索和导航，不承担大段设定正文。
- 若一个域已经有稳定主档，优先从主档进入，不在索引中复制整段说明。

### 2.5 双向链接与结构同步

- 需要稳定关联的笔记之间使用 `[[wikilink]]`。
- 新建或升格一个正式主档时，至少同步检查：
  - 所属域 `_index.md`
  - 父档中的入口
  - 该文件自身的 `关联` 或正文引用
- 不要求为了形式主义把所有轻微提及都做成双向硬链接，但父子结构和稳定入口必须闭环。

---

## 三、模板规则

### 3.1 模板来源

- `new/_TEMPLATES/` 下的实际模板文件，是当前模板骨架的第一来源。
- `plan.md` 负责写规则和例外，不再内嵌整套模板正文，避免计划文本与实际模板脱节。
- 若 `plan.md` 与模板文件发生冲突：
  - 模板字段、顺序、具体骨架以 `new/_TEMPLATES/` 现行为准
  - 结构级规则、目录级规则以本 plan 为准

### 3.2 通用模板要求

- frontmatter 保持轻量，字段只保留当前可稳定判断的项。
- 正文优先使用 2-5 个 section，每个 section 以条目为主，不写长篇散文。
- 模板用于帮助 AI 快速判断“该写什么”，不是为了穷举所有信息栏位。
- 不给所有类型强塞统一的“故事作用”之类字段；是否需要专门栏目，取决于该类型是否长期适用。
- 模板名不等于最终文件名前缀；剧情域尤其按“现有目录层级 + 正式文件名前缀”落地。

### 3.3 当前模板库

当前模板库以 `new/_TEMPLATES/_index.md` 为准，现有模板包括：

- 角色系：`tpl-核心角色主档`、`tpl-其他人物`、`tpl-非人个体`、`tpl-灵感index`、`tpl-灵感事件`
- 世界系：`tpl-地点`、`tpl-种族`、`tpl-神明`、`tpl-物品`、`tpl-组织`、`tpl-生灵`、`tpl-魔法规则`、`tpl-法则`、`tpl-历史纪元`
- 剧情与正文：`tpl-伏笔`、`tpl-分卷纲要`、`tpl-分幕`、`tpl-章节`

当前剧情域的模板映射口径：

- `tpl-分卷纲要` 对应 `vol-第N卷.md`
- `tpl-分幕` 对应 `scene-第N卷-XX-标题.md`
- `tpl-伏笔` 对应 `fore-*.md`
- `tpl-剧情弧` 目前视为预留模板，仅在后续确实需要专题剧情弧文件时启用，不作为当前剧情域默认主文件模板

### 3.4 地点模板的现行规则

`tpl-地点.md` 当前固定使用以下字段与 section：

- frontmatter 顺序：`类型 / 概括 / aliases / 父地点 / 所属势力 / 关联 / 创建 / 更新`
- 正文 section：`位置 / 主要信息 / 下级地点`

地点模板中已经废止以下旧栏位，不再恢复：

- `环境`
- `势力与住民`
- `故事作用`

---

## 四、地点域当前稳定方案

### 4.1 总体原则

- `new/02-世界/地点/_index.md` 是地点域唯一总索引，也是全地点目录树。
- 总索引像书籍目录一样展开上下级，用来解决“这个地点属于哪里”的反查问题。
- 已独立建档的地点使用 `[[wikilink]]`；已命名但暂不独立建档的地点允许以纯文本挂在目录树中。

### 4.2 地点文件的父链规则

每个正式地点文件都必须同时写出直接上级：

- frontmatter `父地点`
- 正文 `## 位置` 第一条中的“上级地点”

这样做的目的，是保证：

- 搜索到单个地点文件时，可以直接知道它归属谁
- 总索引和具体文件的层级表达保持一致
- AI 不必先跳回索引，才能理解地点归属

### 4.3 父档与子地点规则

- 每个父地点主档都在 `## 下级地点` 中只列直接子地点。
- `下级地点/` 目录只放真实地点文件，不再创建任何 `下级地点/_index.md`。
- 地点物理目录按一级地点分组，世界层级主要依靠 `父地点` 与总索引表达，而不是依靠更深层文件夹。
- 只有满足以下任一条件的地点才值得独立建档：
  - 会反复出现
  - 需要独立设定说明
  - 会被多个文件引用
  - 自己还有继续向下展开的必要

### 4.4 当前世界高层结构

地点域当前已固定为“四大陆 + 海上结构”的口径：

- `geo-世界格局`：地点域最高层总览
- 四大陆：`geo-艾瑟维伦`、`geo-卡洛提亚`、`geo-萨弥罗恩`、`geo-诺赫提斯`
- 跨大陆或边缘结构：`geo-中央内海`、`geo-奥尔德林学院`、`geo-群岛与航路带`、`geo-外海危险圈`

其中必须保持以下共识，不得回退：

- `geo-艾瑟维伦` 是正式名，`主大陆板块 / 主舞台板块` 只作为旧称保留。
- 艾瑟维伦远离十二门扉主体压力，只有北方一座门扉长期由重兵把守，因此最富足、最稳定。
- `geo-北方门扉防线` 是 `geo-边疆遗迹区` 下的正式二级地点，用来承接“艾瑟维伦为何更稳定”的结构解释。
- `geo-中央内海` 不再理解为“主大陆附属海域”，而是串联四大陆的核心海域。
- `geo-奥尔德林学院`、`geo-群岛与航路带`、`geo-外海危险圈` 都属于跨大陆或边缘结构，不再附属于旧的单大陆叙事。

### 4.5 当前地点域的工作边界

- 地点总索引应尽量完整，但不强求把所有纯文本节点立刻升格成文件。
- 父档负责交代宏观结构，子档负责向下细化，不要重复大段内容。
- 不为了“看起来完整”而批量新建空白二级地点。
- 如果后续要继续补地点，优先补已经在总索引或父档中稳定出现的节点。

---

## 五、当前建设批次

### 批次 A：地点域二级地点补强（进行中）

- 状态：进行中
- 目标：补会反复出现、需要独立说明、会被多处引用的二级地点
- 输入来源：当前 `new/` 已有地点主档 > memory > `.claude/drafts/` > old vault
- 产出：新的二级地点正式文件、父档 `下级地点` 更新、地点总索引同步收口
- 不做：不批量建空壳城市，不重开 `下级地点/_index.md`，不回到旧地点模板写法
- 验证：新地点能从总索引、父档、文件自身三处被找到，且父链一致

### 批次 B：世界域轻量主档收口（待开始）

- 状态：待开始
- 目标：补齐种族、组织、规则、历史等仍偏空的世界主档
- 输入来源：当前 `new/` 已有内容 > memory > `.claude/drafts/` > old vault
- 产出：轻量但可检索的正式主档，以及对应域 `_index.md` 的简洁入口
- 不做：不把旧库长模板原样搬回，不为了“完整”强行扩写成百科全书
- 验证：每个主档都能回答“这是什么、和谁相关、从哪里继续看”

### 批次 C：角色 / 剧情 / 世界链接收口（待开始）

- 状态：待开始
- 目标：补 `char-* / arc-总纲 / timeline-* / vol-* / scene-* / fore-* / 关系` 与世界设定之间的稳定链接
- 输入来源：当前角色、剧情、世界主档与索引
- 产出：更清晰的跨域 wikilink 和更稳定的角色-地点-剧情关系入口
- 不做：不打破剧透隔离，不把伏笔真相直接塞进当前正史角色卡
- 验证：从角色、剧情或地点任一端出发，都能两三跳内到达相关核心设定

### 批次 D：全库索引与一致性巡检（持续）

- 状态：持续
- 目标：巡检 `_index.md`、frontmatter、父链、旧链接残留、命名一致性
- 输入来源：当前 `new/` 全库
- 产出：索引收口、字段收口、命名统一、明显冲突消除
- 不做：不把巡检变成无边界大重写
- 验证：全库检索结果稳定，主要域入口闭环，不再出现已废止的结构写法

---

## 六、执行 AI 指令模板

把下面这段作为当前模式下的默认执行口径：

```text
你是在现有 `new/` 设定库基础上做补强，不是 old vault 迁移执行者。

开工顺序：
1. 先读对应域的 `_index.md` 确认现状与入口
2. 再读 `new/_TEMPLATES/` 下对应模板
3. 再读当前相关主档
4. 若当前主库信息仍不足，再参考 memory、`.claude/drafts/` 和 old vault

执行要求：
- 优先维护 `new/` 当前结构，不回退到旧目录或旧模板思路
- 改动必须同步维护相关 `_index.md`、父子链和必要的 `[[wikilink]]`
- 若 `plan.md` 与实际模板文件冲突，以 `new/_TEMPLATES/` 的现行模板为准
- 若目录或结构级规则不明确，先回看 `plan.md` 中已经定下的规则
- 剧情域默认沿用现有分层目录，只在正式文件名上应用前缀，不把分卷、分幕改回无层级扁平结构

禁止：
- 为了“补全”批量建立空壳文件
- 恢复已经废止的地点模板旧栏位
- 重建 `下级地点/_index.md`
- 把旧模板逻辑原样抄回当前库
```

补充说明：

- 当前角色灵感页里残留的 `[[arc-角色名]]` 只视为旧占位或未清理链接，不代表剧情域仍以“每角色一个 arc 文件”为正式结构。
- 若后续确实需要角色专题弧，再正式建立对应 `arc-*`；若不需要，则在后续一致性巡检中清理这些残留链接。

---

## 七、验证方法

每轮批次或较大改动完成后，至少检查以下项目：

1. `plan.md` 自身不再以“迁移任务”定义当前工作。
2. 地点规则中明确写有 `父地点` 与 `位置 / 主要信息 / 下级地点`。
3. `plan.md` 中不再出现地点模板旧 section：
   - `环境`
   - `势力与住民`
   - `故事作用`
4. `plan.md` 中明确写出：
   - 四大陆：艾瑟维伦 / 卡洛提亚 / 萨弥罗恩 / 诺赫提斯
   - `北方门扉防线`
   - 地点域只有一个总索引
   - 不再使用 `下级地点/_index.md`
5. `plan.md` 中明确写出剧情域当前前缀化目标：
   - `arc-总纲.md`
   - `timeline-时间线.md`
   - `vol-第一卷.md`
   - `scene-第一卷-01-林地到学院.md`
   - `fore-*`
   - `全局灵感池.md` 是剧情域无前缀例外
6. `plan.md` 中明确写出：
   - 现有 `分卷 / 分幕` 目录保留不变
   - 前缀应用于正式文件名
   - 不把剧情域回退成“人物 arc 文件制”
7. 当前建设批次使用统一口径：
   - 状态
   - 目标
   - 输入来源
   - 产出
   - 不做
   - 验证
8. `plan.md` 与当前现有文件不打架：
   - `new/_TEMPLATES/tpl-地点.md`
   - `new/_TEMPLATES/tpl-分卷纲要.md`
   - `new/_TEMPLATES/tpl-分幕.md`
   - `new/03-剧情/` 当前目录分层
   - `new/02-世界/地点/_index.md`
   - `new/02-世界/_index.md`

---

## 八、参考来源

后续工作默认按以下顺序取材：

1. 当前 `new/` 下的正式文件与索引
2. `new/_TEMPLATES/`
3. memory
4. `.claude/drafts/`
5. old vault

说明：

- old vault 仍然可以提供旧设定、旧命名或遗失信息，但不再主导当前结构设计。
- 是否归档 old vault，作为独立维护问题另行处理，不写入当前总计划正文。
